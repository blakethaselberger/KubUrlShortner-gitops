apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: url-shortener

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app: url-shortener
  managed-by: argocd
